import '@testing-library/jest-dom';
import { render } from '@testing-library/react';
import App from '../App';
import { ErrorBoundary } from '../components';
import { BrowserRouter } from 'react-router-dom';

test('Common render the main page', () => {
  render(
    <BrowserRouter>
      <ErrorBoundary>
        <App />
      </ErrorBoundary>
    </BrowserRouter>
  );
});
